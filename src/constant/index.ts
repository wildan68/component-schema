export enum ColorSchema {
  PRIMARY = '--primary',
  SECONDARY = '--secondary',
  SUCCESS = '--success',
  WARNING = '--warning',
  DANGER = '--danger'
}

export enum Feature {
  GET_PAGE = 'getpage',
  SET_CURRENT_PAGE = 'setCurrentPage'
}